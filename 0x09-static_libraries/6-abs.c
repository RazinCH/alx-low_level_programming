#include "main.h"

/**
 *_abs - Gives the absolute value of an integer
 *
 *@n: int value to check
 *
 * Return: Absolute of int
 */

int _abs(int n)
{
	if (n >= 0)
	{
		return (n);
	}
	return (-n);
}

